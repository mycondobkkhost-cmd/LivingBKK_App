#!/usr/bin/env bash
# CocoaPods wrapper — system Ruby 2.6 on macOS 26 needs explicit Logger load.
set -euo pipefail
export PATH="${HOME}/.gem/ruby/2.6.0/bin:${PATH}"
POD_BIN="${HOME}/.gem/ruby/2.6.0/bin/pod"
exec ruby -r logger "${POD_BIN}" "$@"
